name: Deploy Worker Service

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

env:
  SERVICE_NAME: qwintly-wg-worker
  IMAGE_NAME: qwintly-wg-worker

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_QWINTLY_GIT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: '${{ vars.GCP_PROJECT_ID_QWINTLY }}'

      - name: Configure Docker
        run: |
          gcloud auth configure-docker ${{ vars.REGION }}-docker.pkg.dev

      - name: Build and Push Image
        run: |
          IMAGE=${{ vars.REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID_QWINTLY }}/qwintly-images/${{ env.IMAGE_NAME }}:${{ github.sha }}

          gcloud builds submit \
            --tag $IMAGE \
            .

          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Deploy Cloud Run
        run: |
          gcloud run deploy $SERVICE_NAME \
            --image $IMAGE \
            --region asia-south1 \
            --platform managed \
            --service-account *** \
            --set-secrets-file cloudrun-secrets-config.env \
            --min-instances 1 \
            --max-instances 10 \
            --concurrency 1 \
            --timeout 3600 \
            --cpu 1 \
            --memory 1Gi
